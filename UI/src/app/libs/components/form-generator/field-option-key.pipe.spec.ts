import { FieldOptionKeyPipe } from './field-option-key.pipe';

describe('FieldOptionKeyPipe', () => {
  it('create an instance', () => {
    const pipe = new FieldOptionKeyPipe();
    expect(pipe).toBeTruthy();
  });
});
