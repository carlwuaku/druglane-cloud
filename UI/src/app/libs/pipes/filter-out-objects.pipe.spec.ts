import { FilterOutObjectsPipe } from './filter-out-objects.pipe';

describe('FilterOutObjectsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterOutObjectsPipe();
    expect(pipe).toBeTruthy();
  });
});
